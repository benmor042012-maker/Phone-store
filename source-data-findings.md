# ממצאי בדיקת מאגר התוכן של אתר המקור

## מסקנה

בארכיון המקור **אין מיגרציות SQL, schema של מסד נתונים, או חיבור ל־D1/PostgreSQL/MySQL**. במקום זאת, האתר המקורי הוא Cloudflare Worker סטטי שמנהל את התוכן הדינמי שלו ב־**Cloudflare KV** תחת binding בשם `STORE`.

## מודל אחסון המקור

| רכיב | אופן אחסון | פרטים |
| --- | --- | --- |
| תוכן חנות | Cloudflare KV | מעטפת JSON אחת הנשמרת תחת מפתח תוכן פנימי (`DATA_KEY`) |
| גיבוי קודם | Cloudflare KV | המעטפת הקודמת נשמרת לפני פרסום (`PREV_KEY`) |
| תמונות מנהל | Cloudflare KV | בינארי תחת prefix ייעודי לתמונות |
| נכסים ציבוריים | Cloudflare Workers Assets | וידאו Hero, תמונות מוצרים וקטגוריות תחת `public/assets` |
| אימות מנהל | Cloudflare Worker secrets | `ADMIN_PASSWORD` או `ADMIN_TOKEN_SECRET` |

## ישויות תוכן במעטפת ה־KV

| ישות | שימוש באתר |
| --- | --- |
| `settings` | שם חנות, פרטי קשר, כתובת, שעות ותנאים |
| `heroes` | שקופיות Hero, כותרות, תיאורים וקריאות לפעולה |
| `categories` | עשר קטגוריות, מזהים, אייקונים ותמונות |
| `products` | מזהה מוצר, תמונה, שם, מותג, קטגוריה, מחיר, מחיר קודם, תג, פופולריות ומפרט |
| `reviews` | תוכן לקוחות המוגדר על ידי מנהל האתר במקור |

## נתיבי ה־API הקיימים במקור

| נתיב | פעולה |
| --- | --- |
| `GET /api/data` | מחזיר את מעטפת התוכן מ־KV כאשר קיימת |
| `PUT /api/data` | מפרסם מעטפת תוכן חדשה אחרי אימות מנהל |
| `POST /api/admin/login` | התחברות למנהל |
| `POST /api/upload` | העלאת תמונה ל־KV |
| `GET /img/:id` | הגשת תמונה שמורה מ־KV |

## המשמעות עבור האתר המשוחזר

הפרויקט הנוכחי הוא Frontend סטטי, ולכן הוא לא יכול להתחבר ישירות ובבטחה ל־Cloudflare KV של האתר המקורי. כדי להפוך את המלאי וההגדרות לדינמיים יש לבחור בין שני מסלולים: חיבור Proxy מאובטח ל־API הקיים או שדרוג לאתר מלא עם מסד נתונים פנימי והעברת תוכן מ־KV. אין להריץ מיגרציות, מפני שאין מיגרציות כאלה במקור.
